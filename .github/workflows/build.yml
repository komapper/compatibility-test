name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  kotlin-1_9:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 1.9.24 ]
        ksp: [ 1.9.24-1.0.20 ]
        komapper: [ 1.18.1, 2.2.2 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 11

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          gradle-version: 6.8.3

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=11

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports

  kotlin-2_0:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 2.0.21 ]
        ksp: [ 2.0.21-1.0.28 ]
        komapper: [ 2.0.0, 5.2.1 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 11

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          gradle-version: 7.6.4

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=11

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports

  kotlin-2_1_21:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 2.1.21 ]
        ksp: [ 2.1.21-2.0.2 ]
        komapper: [ 5.0.0, 5.7.0 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 11

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          gradle-version: 7.6.4

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=11

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports

  kotlin-2_2:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 2.2.20 ]
        ksp: [ 2.2.20-2.0.3 ]
        komapper: [ 5.0.0, 5.7.0 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 11

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          gradle-version: 7.6.4

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=11

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports

  kotlin-2_3_0:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 2.3.0 ]
        ksp: [ 2.3.9 ]
        komapper: [ 5.7.0, 6.1.0 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 11

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          # Standalone KSP (2.3.0+) needs Gradle 8.2+: the Kotlin DSL in older
          # Gradle cannot read the KSP plugin jar's Kotlin 2.3 metadata.
          gradle-version: 8.2.1

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=11

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports

  kotlin-2_3:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 2.3.21 ]
        ksp: [ 2.3.9 ]
        komapper: [ 7.0.0 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          # Standalone KSP (2.3.0+) needs Gradle 8.2+: the Kotlin DSL in older
          # Gradle cannot read the KSP plugin jar's Kotlin 2.3 metadata.
          gradle-version: 8.2.1

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=17

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports

  kotlin-2_4:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        kotlin: [ 2.4.0 ]
        ksp: [ 2.3.9 ]
        komapper: [ 7.0.0 ]
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up JDK
        uses: actions/setup-java@b6effb05e454b25005698d916606bdc6ffcbf961 # v5.7.0
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@9c971963bec38e04b3d30dcc455b5382be2fdbfb # v6.3.0
        with:
          # Standalone KSP (2.3.0+) needs Gradle 8.2+: the Kotlin DSL in older
          # Gradle cannot read the KSP plugin jar's Kotlin 2.3 metadata.
          gradle-version: 8.2.1

      - name: Assemble and Check
        run: >
          gradle
          assemble
          check
          -PkomapperVersion=${{ matrix.komapper }}
          -PkotlinVersion=${{ matrix.kotlin }}
          -PkspVersion=${{ matrix.ksp }}
          -PjvmTargetVersion=17

      - name: Upload reports
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: reports-${{ github.job }}-${{ matrix.kotlin }}-${{ matrix.ksp }}-${{ matrix.komapper }}
          path: ./**/build/reports
